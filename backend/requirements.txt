# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9

# Audio processing
librosa==0.10.2
numpy>=1.24.0
# essentia is optional (better BPM detection) — install manually if available:
#   pip install essentia
# Falls back to librosa-only BPM detection if not installed.

# Vocal separation (optional — requires torch)
# demucs provides AI vocal isolation. Falls back to using original audio if not installed.
#   pip install demucs
# demucs==4.0.1

# Speech recognition (ASR) + forced alignment (optional — requires torch)
# WhisperX = Whisper + wav2vec2 forced alignment for ~50ms word accuracy
# Falls back to vanilla Whisper if WhisperX unavailable
# Falls back to user-provided lyrics if neither is installed
#   pip install whisperx openai-whisper
# whisperx>=3.8.0
# openai-whisper

# MIDI
mido==1.3.2

# Syllable splitting
pyphen==0.16.0

# Data validation (FastAPI dependency)
pydantic>=2.0.0
